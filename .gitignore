# venv + caches
.venv/
__pycache__/
*.pyc
*.pyo

# Model artifacts are checked in so the pipeline is runnable after cloning.
# Keep unrelated model files out of git.
*.pth
*.onnx
*.xml
*.bin
!models/
!models/*

# video + outputs (test.mp4 is the source; annotated outputs + tracks are derivable)
*.mp4
out*/
tracks.json

# run logs: transients from tools/run_test_video.py redirects
*.log
